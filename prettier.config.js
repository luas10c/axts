/** @type{import('prettier').Config} */
const config = {
  singleQuote: true,
  semi: false,
  trailingComma: 'none',
  arrowParens: 'always',
  bracketSpacing: true,
  bracketSameLine: false,
  printWidth: 90,
  useTabs: false,
  tabWidth: 2,
  plugins: []
}

export default config
