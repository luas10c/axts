/** @type{import('prettier').Config} */
const config = {
  semi: false,
  singleQuote: true,
  arrowParens: 'always',
  trailingComma: 'none',
  bracketSpacing: true,
  endOfLine: 'lf',
  printWidth: 80,
  tabWidth: 2,
  useTabs: false,
  plugins: []
}

export default config
